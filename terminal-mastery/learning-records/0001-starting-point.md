# Starting point: zero Vim, working stack, AI-first workflow

Sandeep has never used modal editing and knows a little Python. He has already forked
`kunchenguid/dotfiles` to `sandeepsalwan1/dotfiles`, adapted it heavily (~800 lines: a
60-item brew list, split zsh files, git identity, a `scripts/dotfiles` CLI), and
successfully applied it - five nix-darwin generations, all nine Neovim plugins cloned,
herdr 0.8.2 live.

So the floor is: comfortable editing config and running commands, afraid of the editor
itself. Teach *usage*, never installation. Weight everything toward reading and judging
AI-written code rather than authoring it, because that is the stated workflow.
